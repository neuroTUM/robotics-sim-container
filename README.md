# robotics-sim-container
This is a repository that contains the dockerfile and setup pipeline for the container, and automatically builds the image to publish to GHCR, as a base for the robotics simulation DevContainer
